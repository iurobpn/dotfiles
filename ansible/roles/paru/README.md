Role Name
=========

A role to install paru package in a archlinux.

Role Variables
--------------

the pkgs variable list contains xorg related packages to install

Example Playbook
----------------

Including an example of how to use your role (for instance, with variables passed in as parameters) is always nice for users too:

    - hosts: servers
      roles:
         - paru

