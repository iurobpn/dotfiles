---
galaxy_info:
  author: "Iuro Nascimento"
  description: "Installs basic Rust packages via Cargo (e.g., ripgrep and rd-find)."
  license: "MIT"
  min_ansible_version: "2.9"
  platforms:
    - name: "Ubuntu"
      versions:
        - "noble"
  galaxy_tags:
    - cargo
    - rust-packages

dependencies:
  - role: rustup
