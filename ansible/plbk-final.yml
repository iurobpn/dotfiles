---
# - hosts: 
#     - local
#   become: yes
#   roles:
#
#     - luapath
    # - rustup
    # - rust-pkgs

- name: Run additional tasks after roles
  hosts: 
    - local
  become: yes
  tasks:
    - name: Extra configuration task
      shell: |
        "{{ home_dir }}/git/dotfiles/bin/setup.sh"
      args:
        chdir: "{{ home_dir }}/git/dotfiles"

