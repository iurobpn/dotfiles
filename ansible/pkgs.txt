dhcpcd # network management daemon
sudo # alter visudo


xorg-server xorg-apps xorg
xf86-video-nouveau

xfce4 xfce4-goodies
archlinux-xdg-menu

dhcpcd # network management daemon for ethernet
networkmanager # network management daemon for wifi

add multilib and arch4edu
add paru
mkdir aur
sudo pacman -S base-devel git cargo wget curl
git clone https://aur.archlinux.org/paru.git
cd paru
makepkg
sudo pacman -U paru*.pkg.tar.zst


texlive
wget https://mirror.ctan.org/systems/texlive/tlnet/install-tl-unx.tar.gz
tar -xvf install-tl-unx.tar.gz -C /tmp
cd /tmp/install-tl-*
sudo perl ./install-tl --no-interaction

echo 'export PATH=$PATH:/usr/local/texlive/2025/bin/x86_64-linux' >> ~/.bashrc
echo 'export PATH=$PATH:/usr/local/texlive/2025/bin/x86_64-linux' >> ~/.zshrc
# add manpath and infopath commands

arch4edu
sudo pacman -S gnupg
sudo pacman-key --recv-keys 7931B6D628C8D3BA
sudo pacman-key --finger 7931B6D628C8D3BA
sudo pacman-key --lsign-key 7931B6D628C8D3BA
sudo bash -c 'echo "[arch4edu]" >> /etc/pacman.conf'
#bash -c 'echo "Server = https://mirror.lesviallon.fr/arch4edu/$arch" >> /etc/pacman.conf'
sudo bash -c 'echo "Server = https://repository.arch4edu.org/$arch" >> /etc/pacman.conf'
sudo pacman -Syu
sudo pacman -S pkgstats
sudo pacman -S xclip
paru -S wezterm-git

ttf-hack-nerd
fish
starship
tmux
task
timew
tldr
unzip

# generate ssh keys
openssh

ssh-keygen
...

xdotool
inetutils #hostname
xarchiver
nerd-fonts-emoji # for emoji support in terminal

mkdir -p ~/.local/share/nvim/ggn
ln -s ~/git/tasks.nvim ~/.local/share/nvim/ggn/tasks.nvim


#how to start xfce at boot?
#use lightdm and lightdm-gtk-greeter, start lightdm service
# configure at /etc/lightdm/lightdm.conf at [seat:*] session
greeter-session=lightdm-gtk-greeter

arcanisrepo - scientific repo

sudo pacman-key --recv-keys E989490C
sudo pacman-key --finger E989490C
sudo pacman-key --lsign-key E989490C
sudo bash -c 'echo "[arcanisrepo]" >> /etc/pacman.conf'
sudo bash -c 'echo "Server = https://repo.arcanis.me/arcanisrepo/x86_64" >> /etc/pacman.conf'
sudo bash -c 'echo "SigLevel = DatabaseRequired PackageNever TrustedOnly" >> /etc/pacman.conf'
