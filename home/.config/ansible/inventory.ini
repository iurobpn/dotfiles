[remote_servers]
# Replace 203.0.113.10 with your server's fixed IPv4 address and adjust the username if needed.
dovpn
# Optionally, if you need to specify a key, uncomment the next line and set the correct path.
# server1 ansible_host=203.0.113.10 ansible_user=ubuntu ansible_ssh_private_key_file=/path/to/your/private/key

[local]
localhost ansible_connection=local
